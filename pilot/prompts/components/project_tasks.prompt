私達がコーディング作業に入る前に、この{{ task_type }}を作成する開発プロセスを小さなタスクに、あなたが分割してほしい。そうすれば、開発、デバッグが容易になり、{{ task_type }}を動作させることが容易になる。{{ task_type }}を分割したそれぞれの小さなタスクは、{{ task_type }} を完成させるために正しい方向に進んでいることを、開発者がレビューできるものでなければならない。

それぞれのタスクは、この{{ task_type }}の開発だけに関連するものでなければならず、それ以外のものであってはならない。{{ task_type }} が完全に動くようになったら、それで終わりとする。リサーチ、デプロイ、ドキュメントの作成など、実際のコードを書く以外のタスクはあってはならない。

提示されたプロジェクト詳細{% if task_type  == 'feature' %} と新機能の説明{% endif %}に基づいて、タスクごとに考えて、{% if task_type  == 'feature' %}新機能の{% endif %}全体開発計画を策定して下さい。{% if task_type  == 'app' %}プロジェクトのセットアップから開始し、アプリ全体が完全に動作するようになるまで、各タスクを定義します。{% endif %}

**重要**
プロジェクトをタスクに分割することは非常に重要な仕事であり、以下に挙げるすべてのルールを常に厳守しながら慎重に行わなければならない。

ルール #1
テストするだけのタスクや、何かが動作することを確認するだけのタスクがあってはならない。すべてのタスクにはコーディング作業が含まれていなければならない。どのタスクにも言えることだが、プロジェクトの最後のタスクでは特に重要である。 {{ task_type }} が動くかどうかのテストは、各タスクの一部として行われる。

ルール #2
プロジェクトが、理由もなく小さすぎたり単純すぎたりするタスクに分割されないようにしなければならない。また、開発、デバッグ、レビューが難しくなるような大きすぎたり複雑すぎたりするタスクにも分割されないようにしなければならない。以下は、不十分なタスクの例である:

**単純すぎるタスク**
- Node.jsプロジェクトをセットアップし、必要な依存関係をすべてインストールする。
- Mongoose を使って、IP '127.0.0.1' で MongoDB データベース接続を確立する。

**複雑すぎるタスク**
- ユーザー認証、ユーザースキーマを持つMongoDBへの接続、新規ユーザーのメーリング、素敵なデザインのフロントエンドを持ち、/home、/profile、/register、/loginルートがあるNode.jsプロジェクトを立ち上げる。

あなたは、単純すぎたり複雑すぎたりするタスクを作成することを避けなければならない。あなたは、中程度の複雑さのタスクを作ることを目指さなければならない。以下は良いタスクの例です。:

**良いタスク**
- Node.jsプロジェクトをセットアップし、必要な依存関係をすべてインストールする。ステータス200を返す`/ping`への単純なルートでexpressサーバーをセットアップする。
- MongoDB データベース接続を確立し、メッセージの永続的な保存のために Mongoose を使ってメッセージスキーマを実装する。

ルール #3
それぞれの{{ task_type }}は、複雑さに応じて、タスク数が異なるはずである。タスクごとに考えて、この特定の{{ task_type }}に適切な最小限のタスクを作成して下さい。{% if task_type  == 'feature' %} もし機能が小さければ、タスクは1つだけでもよい。{% endif %} タスクをいくつ作るべきかの指針になるような、複雑さの異なるアプリの例をいくつか紹介する。:

例 #1:
アプリの説明: "localhost:3000で開くと'Hello World'とだけ表示されるアプリを作りたい。"
タスク数: 1-3

例 #2:
アプリの説明: "node.jsアプリを作成して、ユーザーがアプリにユーザー登録したりログインしたりできるようにする。フロントエンドには、/home（ユーザー・データを表示）、/register、/loginが必要。ユーザーがログインした状態を維持するためにセッションを使用する。"
タスク数: 2-6

例 #3:
アプリの説明: "かっこいいオンライン靴屋。データモデルとしては、靴、カテゴリー、ユーザープロファイルがある。ウェブページでは、商品一覧、商品詳細、ショッピングカート機能を提供。見た目はかっこよくて洗練されていなければならない。"
タスク数: 5-15