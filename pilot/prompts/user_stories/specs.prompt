私は、以下に説明するような{{ app_type }} (それを"{{ name }}"と呼ぼう)を作りたい。:
```
{{ prompt }}
```

{% if clarifications and clarifications|length > 1 %}
以下に、{{ app_type }}の説明を明確にするためのいくつかの追加質問と回答を記載する。:
```
{% for clarification in clarifications[2:] %}
    {% if loop.index is odd %}
    Q: {{ clarification.content }}
    {% else %}
    A: {{ clarification.content }}
    {% endif %}
{% endfor %}
```
{% endif %}

 {{ app_type }} "{{ name }}" の説明、追加の質問と回答を、ステップバイステップで考えて、ユーザーストーリーを分解しなさい。あなたは{{ app_type }}の説明とリストアップされた回答について考え、すべてのユーザーストーリーのリストを作成する。ユーザーストーリーは、ユーザーが{{ app_type }}とどのようにやり取りできるかを記述したものである。


例えば、あるアプリの説明文が「チャンネル名に "test "という単語が含まれるYoutubeチャンネルを見つけるスクリプトを作成する」だった場合、ユーザーストーリーは次のようになる。:
- `ユーザーはCLIからスクリプトを実行する。`
- `ユーザーはCSVファイルで全てのチャネルの一覧を取得する。`

**IMPORTANT**
Return one user story at the time. Do not return anything else but single user story. I might ask you to modify some user stories and only when I send you empty response you can move to next user story.

**IMPORTANT**
Once you are done creating all user stories, write the response containing nothing else but this:
{{END_RESPONSE}}