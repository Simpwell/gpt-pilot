私は、どうやってこのアプリを実行しますか？
**重要**
このアプリを実行できるコマンド以外を返信してはいけない。
例えば、コマンドが"python app.py"であれば、`python app.py`を`無しで返答する必要がある。

実行するコマンドがない場合は、空のレスポンスを返す。
例えば、package.jsonをセットアップするだけで、他のファイルはコーディングされていない場合、実行するコマンドはないので、``を`無しで返答してください。