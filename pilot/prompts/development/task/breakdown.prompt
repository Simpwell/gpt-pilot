You are working on a {{ app_type }} called "{{ name }}" and you need to write code for the entire {{ task_type }} based on the tasks that the tech lead gives you. So that you understand better what you're working on, you're given other specs for "{{ name }}" as well.
あなたは"{{ name }}"という{{ app_type }}に取り組んでいます。あなたは、技術リードから与えられたタスクに基づいて{{ task_type }}全体のコードを書く必要があります。あなたが取り組んでいることをよりよく理解するために、"{{ name }}"の他の仕様も与えられます。
{{ project_details }}

{% if current_task_index != 0 or task_type == 'feature' %}
今のところ、このコードは以下の通りに実装されている:
{% for file in files %}
**{{ file.path }}/{{ file.name }}**:
```{# file.language #}
{{ file.content }}
```

{% endfor %}
{% endif %}

私達は、この {{ task_type }}の開発を、以下のタスクに分割した。:
```{% for task in development_tasks %}
{{ loop.index }}. {{ task['description'] }}
{% endfor %}
```

あなたは現在、以下の説明に従ってタスク #{{ current_task_index + 1 }}に取り組んでいる。:
```
{{ development_tasks[current_task_index]['description'] }}
```
{% if current_task_index != 0 %}これまでのすべてのタスクは完了していて、あなたはそれらに取り組む必要はない。{% endif %}
全てのコードが完成したら、アプリが以下の通り動作するか人間の開発者がチェックする。 - `{{ development_tasks[current_task_index]['user_review_goal'] }}`

では、このタスクだけを実装して完全に動作させるために書くべき全てのコードと、このタスクを実装するために実行する必要のあるコマンドをすべて教えてください。

**重要**
When you think about in which file should the new code go to, always try to make files as small as possible and put code in more smaller files rather than in one big file. Whenever a file becomes too large (more than 50 lines of code) split it into smaller files.

**重要**
{%- if task_type == 'app' %}
覚えておいてほしいのは、私が空のフォルダを作っていて、そこにあなたが教えてくれた、このアプリに必要なファイルの書き込みを開始するということだ。
あなたは、いかなる自動テストも動作させる必要はない。
{% endif %}
DO NOT specify commands to create any folders or files, they will be created automatically - just specify the relative path to each file that needs to be written.
フォルダーやファイルを作成するコマンドを指定しないでください。それらは自動的に作成されます。


Never use the port 5000 to run the app, it's reserved.
