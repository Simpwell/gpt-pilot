{#あなたは、現在の修正をコードベースに実装する必要があります。:
-- 指示 --
{{ instructions }}
-- 指示終わり --
#}{% if step_index != 0 %}今までのところ、ステップ{{ finished_steps }}が完了したので、{% else %}{% endif %} ステップ #{{ step_index }}を開始しよう。 - `{{ step_description }}`.

{# 私は修正が必要な各ファイルをあなたに渡すので、あなたは、指示に従ってファイルを修正してください。 #} そのためには、あなたは、現在実装されているファイルを見る必要があるので、まず、上記のうち、指示に関連するファイルをフィルタリングしてください。それから、コードに適切な変更を加えるために必要なファイルを教えてください。ファイルが必要ない場合（ファイルを作成する必要がある場合など）は、空の配列を返すだけでよい。
{#
現在のフォルダーツリーは以下の通り:
```
{{ directory_tree }}
```
#}
プロジェクトルートからの相対パスでファイルを指定してください。 例えば、パス`{project_root}/models/model.py`のファイルが必要な場合、あなたはファイル `models/model.py` をリクエストする必要があります。