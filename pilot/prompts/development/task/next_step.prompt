{% if step_index != 0 %}
これまでに、ステップ{{ finished_steps }} が完了したので、
{% else %}
{% endif %} ステップ #{{ step_index }}を開始しよう。このステップの説明は以下の通りだ。:
```
{{ step_description }}
```
このステップを完遂するために何をすべきか、ステップバイステップで考えてください。
{% if step_type == 'COMMAND' %}
このステップを遂行するために実行する必要のあるすべてのコマンドを返答してください。
{% elif step_type == 'CODE_CHANGE' %}
まず、あなたは、現在書かれているコードを知る必要がある。そうすることで、あなたは、新しいコードを書いたり、既存のコードを更新したりすることが適切にできる。
 {# 以下に、これまでに書かれたすべてのファイルをファイルツリー形式で示す。:
```
{{ directory_tree }}
```
#}
あなたが現在のステップのコードを書く前に見る必要のあるファイルのリストを返答してください。このリストは、各項目がファイル名であるJSON配列である必要があります。指定されたJSON配列以外では応答しないでください。


{% endif %}
