あなたは"{{ name }}"という{{ app_type }}に取り組んでおり、新機能のサマリーを作成する必要がある。
以下は"{{ name }}"のハイレベルな説明である。:
```
{{ app_summary }}
```

{{ app_type }}のコーディングに成功した後、ユーザーから改善を求められた。以下はユーザーが求めた内容である。

:
```
{{ feature_description }}
```

そして、その機能の開発計画が作成された。その機能は開発しやすいように小さなタスクに分解された。これが開発計画だ。:
```{% for task in development_tasks %}
- {{ loop.index }}. {{ task['description'] }}
{% endfor %}
```

あなたのゴールは、できるだけ少ない文章で、この機能のみを説明するサマリーを作成することである。できるだけ短くまとめること。

あなたはプロジェクトやタスクのサマリーを作成するのではない。この機能の要約だけを作成する。

{{summary_instructions}}