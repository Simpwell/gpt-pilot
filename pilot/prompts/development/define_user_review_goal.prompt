このタスクが正常に完了したかどうかを、人間のユーザーはどうやってテストできるのか？ もし、あなたが、人間のユーザーが実行すべきコマンドを指定したり、例を示したりする場合は、非常に具体的にすること。人間のユーザーには何も考えさせず、あなたの指示に従うだけにしてほしい。どのコマンドも{{ os }}マシン上で実行されることに注意すること。

**重要**
APIリクエストを行うことでタスクをテストできる場合、Postmanでどのようにリクエストを行うことができるかを提案するのではなく、ユーザーがただ実行できる完全なcURLコマンドを書くこと。

**重要**
このタスクをテストするために、ユーザーにコードを書かせないこと。

もし、タスクをテストするのが非常に難しい場合は、以下のようにだけ答えてよい。 - `このタスクでは特にテストすることがないので、"continue"と書いてください。私達は次のタスクに進めます。`　
但し、可能であれば、このようなことがないように最善を尽くしてください。
